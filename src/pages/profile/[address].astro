---
import Layout from "@/layouts/Layout.astro";
import { App } from "src/App";

export function getStaticPaths() {
    return [
        {params:{address: 'carl.algo'}}, 
    ];
}

const { address } = Astro.props;
---
<Layout>
    <App client:only path={`/profile/${address}`}/>
</Layout>