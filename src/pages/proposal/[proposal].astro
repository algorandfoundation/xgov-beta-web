---
import Layout from "@/layouts/Layout.astro";
import { App } from "src/App";

const { proposal } = Astro.params;

---
<Layout>
    <App
        client:only
        path={`/proposal/${proposal}`}
    />
</Layout>